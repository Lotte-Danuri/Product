server:
  port: 0

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://ssd018.synology.me:8806/product
    username: product
    password: Asdf123!@#
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
    properties:
      hibernate:
        format_sql: true
  application:
    name: Product
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  main:
    allow-bean-definition-overriding: true

cloud:
  aws:
    credentials:
      accessKey: AKIAZT7GWYCN6ZFOZIVK
      secretKey: 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
    s3:
      bucket: danuri-luxon
      dir: images/
    region:
      static: ap-northeast-2
    stack:
      auto: false

eureka:
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://43.200.203.135:8761/eureka

gateway:
  ip: 43.200.203.135

danurikafka:
  ip: 43.200.203.135:9092
